<center>
   <%if current_user.id != @user.id %>
      <center>
      <%if !current_user.following?(@user)%>
          <%= react_component("followBtn", following: false, user_id: @user.id) %>
       <%else%>
          <%= react_component("followBtn", following: true, relation_id: @user_relation.id, user_id: @user.id) %>
      <%end%>
      </center>
    <%end%>
</center>
